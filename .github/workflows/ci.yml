name: CI 
on: 
  push: 
    branches: [ "main", "feature/*" ] 
jobs: 
  build: 
    runs-on: ubuntu-latest 
    steps: 
    - uses: actions/checkout@v3 
    - name: Run tests 
      run: echo "Run your CI tests here" 
